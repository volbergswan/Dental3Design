# Dental3Design — Client App

Plateforme de conception numérique dentaire pour les laboratoires et cabinets.

## Setup

1. `npm install`
2. Copier `.env.example` en `.env.local` et remplir les clés
3. `npm run dev`
